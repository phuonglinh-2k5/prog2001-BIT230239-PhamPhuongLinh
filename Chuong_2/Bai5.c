#include <stdio.h>

int main() {
    const int tuoi = 21;
    const float chieuCao = 1.56;
    const char truong[] = "Dai hoc CMC";

    printf("Tuoi: %d\n", tuoi);
    printf("Chieu cao: %.2f\n", chieuCao);
    printf("Truong: %s\n", truong);

    return 0;
}