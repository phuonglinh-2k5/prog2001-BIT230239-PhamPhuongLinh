#include <stdio.h>

int main() {
    const int tuoi = 21;
    const float chieuCao = 1.56;
    const char truong[] = "Dai hoc CMC";

    printf("%d, %.2f, %s\n", tuoi, chieuCao, truong);

    return 0;
}